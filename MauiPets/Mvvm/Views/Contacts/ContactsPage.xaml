<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiPets.Mvvm.Views.Contacts.ContactsPage"
             xmlns:models="clr-namespace:MauiPetsApp.Core.Application.ViewModels;assembly=MauiPets.Core"
             xmlns:viewmodel="clr-namespace:MauiPets.Mvvm.ViewModels.Contacts"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:resx="clr-namespace:MauiPets.Resources.Languages"
             xmlns:local="clr-namespace:MauiPets.Mvvm.ViewModels.Contacts"
             x:DataType="local:ContactsViewModel"
             Title="{resx:Localize Key=TituloContactos}">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior Command="{Binding GetContactsCommand}" EventName="Appearing" />
    </ContentPage.Behaviors>
    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding AddContactCommand}"
                     IconImageSource="add.png" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout>
            <Grid RowDefinitions="Auto,Auto,*,Auto" ColumnDefinitions="0.70*,0.30*">
                <Border Grid.Row="0"
                        Grid.ColumnSpan="2"
                        WidthRequest="90"
                        HeightRequest="90"
                        Padding="2"
                        Stroke="#C5C5C5"
                        StrokeThickness="2"
                        StrokeShape="RoundRectangle 45"
                        BackgroundColor="Transparent"
                        HorizontalOptions="Center"
                        VerticalOptions="Center">
                    <Frame Padding="0"
                           CornerRadius="43"
                           HasShadow="False"
                           BackgroundColor="Transparent">
                        <Frame.Clip>
                            <RoundRectangleGeometry CornerRadius="43" Rect="0,0,86,86" />
                        </Frame.Clip>

                        <Image Source="contacts.png"
                               Aspect="AspectFill"
                               WidthRequest="86"
                               HeightRequest="86" />
                    </Frame>
                </Border>

                <SearchBar Grid.Row="1" Grid.Column="0"
                           FontSize="14"
                           Placeholder="{resx:Localize Key=TituloPesquisarContacto}"
                           Margin="5,5,5,25"
                           x:Name="searchBar"
                           WidthRequest="300">
                    <SearchBar.Behaviors>
                        <toolkit:EventToCommandBehavior EventName="TextChanged"
                                                        Command="{Binding SearchContactsCommand}" 
                                                        CommandParameter="{Binding Text, Source={x:Reference searchBar}}"/>
                    </SearchBar.Behaviors>
                </SearchBar>

                <ListView x:Name="ContactsList" x:DataType="models:ContactoVM"
                          Grid.Row="2"
                          Grid.ColumnSpan="2"
                          BackgroundColor="Transparent"
                          SeparatorColor="Green"
                          ItemSelected="OnItemSelected"
                          SelectionMode="Single"
                          HasUnevenRows="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Border Stroke="LightGray" StrokeThickness="1" Margin="8,4">
                                    <Grid Padding="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Label Grid.Row="0" 
                                               Text="{Binding Nome}" 
                                               TextColor="Black"
                                               FontFamily="PoppinsBold" 
                                               FontSize="14"
                                               LineBreakMode="WordWrap"
                                               MaxLines="2"
                                               VerticalOptions="Center"
                                               HorizontalOptions="Start"/>
                                        <Label Grid.Row="1"
                                               Margin="0,5,0,0"
                                               Text="{Binding Movel}" 
                                               FontSize="12" 
                                               TextColor="Green"
                                               FontFamily="PoppinsBold" 
                                               CharacterSpacing="2.2"/>
                                    </Grid>
                                </Border>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Grid Grid.Row="3" Grid.ColumnSpan="2"
                      HorizontalOptions="Center" VerticalOptions="Center">
                    <ActivityIndicator
                        x:Name="ai" 
                        IsRunning="{Binding IsBusy}" 
                        HorizontalOptions="Center" 
                        VerticalOptions="Center" 
                        BackgroundColor="Transparent"
                        Color="#03b48b"/>
                </Grid>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>